function SectionTitle({ title }) {
    return (
        <h2 style={{ display:'flex', justifyContent:'center', padding: '10px 20px '}}>
            {title}
        </h2>
    );
}

export default SectionTitle;